# Core FastAPI dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# CORS support (already included in FastAPI, but explicit for clarity)
# fastapi.middleware.cors is part of FastAPI

# Document processing
unstructured>=0.10.0

# Additional useful dependencies for FastAPI projects
python-multipart>=0.0.6  # For form data and file uploads
pydantic>=2.0.0  # Data validation (included with FastAPI but explicit)

# Optional but commonly used
requests>=2.31.0  # For HTTP client functionality
python-dotenv>=1.0.0  # For environment variable management

# Development dependencies (uncomment if needed)
# pytest>=7.4.0
# pytest-asyncio>=0.21.0
# httpx>=0.25.0  # For testing FastAPI endpoints
